export interface PropTypes {
  children: React.ReactNode;
  delay?: number;
}

export interface ProductPropTypes {
  src: string;
  alt: string;
  title: string;
  description: string;
  delay?: number;
}
